# ==========================================
# 环境变量配置示例
# 复制此文件为 .env 或 .env.local 使用
# ==========================================

# Application Configuration
VITE_APP_TITLE=MarkFlow
VITE_APP_DESC=Markdown Workspace
VITE_BASE_URL=/markflow/

# Router Configuration
VITE_ROUTER_HISTORY_MODE=history
# static: routes from generatedRoutes + menu-groups.ts (dev fallback)
# dynamic: sidebar and auth routes from menu mock/API (menu management module)
VITE_AUTH_ROUTE_MODE=dynamic
VITE_ROUTE_HOME=home

# Icon Configuration
VITE_ICON_PREFIX=icon
VITE_ICON_LOCAL_PREFIX=local-icon
VITE_MENU_ICON=mdi:menu

# Backend Service Configuration
VITE_SERVICE_BASE_URL=http://localhost:3000

# ==================== 业务状态码配置 ====================
# 注意：以下配置只能使用业务错误码（code 字段），不能使用 HTTP 状态码

# 成功状态码（逗号分隔，无空格）
# 200 = SUCCESS (通用成功)
# 201 = CREATED (创建成功)
VITE_SERVICE_SUCCESS_CODE=200,201

# Token 过期但可刷新的错误码（逗号分隔，无空格）
# 1202 = TOKEN_EXPIRED (Access token has expired)
# 当收到此错误码时，系统会自动调用 /api/admin/auth/refresh 刷新 token
# 刷新成功后会重新发送原请求，刷新失败则登出
VITE_SERVICE_EXPIRED_TOKEN_CODES=1202

# 需要立即登出的错误码（逗号分隔，无空格）
# 1200 = AUTHENTICATION_FAILED (认证失败)
# 1201 = INVALID_CREDENTIALS (凭证无效)
# 1203 = TOKEN_INVALID (Token无效)
# 1204 = TOKEN_REVOKED (Token已撤销)
# 1205 = SESSION_EXPIRED (会话已过期)
# 注意：不包括 1202，因为 1202 会尝试刷新 token
VITE_SERVICE_LOGOUT_CODES=1200,1201,1203,1204,1205

# 需要显示模态框提示的登出错误码（逗号分隔，无空格）
# 1206 = ACCOUNT_LOCKED (账号已锁定)
# 1207 = ACCOUNT_DISABLED (账号已禁用)
# 2000 = ACCOUNT_BLACKLISTED (账号已列入黑名单)
# 这些错误会先显示模态框告知原因，用户点击确认后再执行登出
VITE_SERVICE_MODAL_LOGOUT_CODES=1206,1207,2000

# ==================== 其他配置 ====================

# Other Service Base URLs (JSON format)
VITE_OTHER_SERVICE_BASE_URL={"demo":"http://localhost:4000"}

# HTTP Proxy (Y/N) - Only for development
VITE_HTTP_PROXY=Y

# Show proxy logs in terminal (Y/N)
VITE_PROXY_LOG=Y

# Static Super Role
VITE_STATIC_SUPER_ROLE=super_admin

# Local dev without backend: auto bypass login (default in `pnpm dev`)
# Set to N when you need to test real login against VITE_SERVICE_BASE_URL
# VITE_DEV_BYPASS_AUTH=N

# Force static-demo mode (same as GitHub Pages build); optional if dev bypass is enough
# VITE_STATIC_DEMO=Y

# Build with sourcemap (Y/N)
VITE_SOURCE_MAP=N

# Drop console statements in production (Y/N)
# When set to Y, console.log, console.warn, console.error, console.info, console.debug, and debugger statements
# will be removed from production builds to reduce bundle size and hide debug information
VITE_DROP_CONSOLE=Y

# Storage prefix for different domains
VITE_STORAGE_PREFIX=admin_

# Automatically detect updates (Y/N)
VITE_AUTOMATICALLY_DETECT_UPDATE=Y

# Iconify API provider (optional)
# VITE_ICONIFY_URL=https://api.iconify.design

# Launch editor for DevTools (optional)
# VITE_DEVTOOLS_LAUNCH_EDITOR=code

